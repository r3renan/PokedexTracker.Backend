const express = require('express');
const PokemonController = require('./controllers/PokemonController');

const routes = express.Router();

/**
 * Definição de rotas da aplicação aqui.
 */

module.exports(routes);